Re-sync this workspace to the latest of GitHub branch claude/ecstatic-wright-y4npO
(head c334a18), same method as before (native GitHub pull / Contents API).

This is a FRONTEND-ONLY change — a design cohesion pass on the signed-in app.
No new dependencies, NO database migration, NO new secrets, no engine/prompt
changes, no API changes.

What changed (UI only):
- New shared component artifacts/still/src/components/page.tsx (PageHeader) so
  every signed-in screen opens the same way (large serif title + quiet subtitle
  + optional right-aligned action). Adopted on Library, Returns, and Settings.
- Standardized content width to 680px across the app (Library was 720;
  Settings/Notifications were 640) so the screens line up.
- Library: year groups are now quiet "shelf label" dividers instead of large
  titles that competed with the page heading.
- Import: the paste box now sits on the same anchored "sheet" surface as Today.
- The unverified-email banner is warmed to an intentional sepia tint.

Steps:
1. SYNC the code to origin (head c334a18). Do not edit app code.
2. pnpm install (no-op; no new deps).
3. RE-BUILD the client and RE-PUBLISH the deployment so the live app serves the
   updated screens. (The api-server serves the built SPA in production.)
   - If you build manually: the client build is
       pnpm --filter @workspace/still run build
     (it expects PORT and BASE_PATH in the env per the existing vite config;
     e.g. PORT=5173 BASE_PATH=/ — these only affect the build, not runtime).
4. VERIFY in the browser after publish:
   - Sign in and click through Today / Library / Returns / Settings / Reminders
     / Import — they should now share one header style and the same 680px width.
   - Library: year rows read as small uppercase shelf labels with a hairline.
   - Import: the paste area looks like Today's writing "sheet."

No DB push, no secrets, no cron changes needed for this sync. Report what you
ran and anything that looked off. Do not change application code or prompts.
