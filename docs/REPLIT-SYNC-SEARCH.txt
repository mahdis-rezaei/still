Re-sync this workspace to the latest of GitHub branch claude/ecstatic-wright-y4npO
(head b5d2881), same method as before. CODE-ONLY (frontend) — NO migration, NO
new endpoints, NO new secrets.

What lands (completes Batch 3 Tier A):
  - /search — "Search your life": a dedicated, client-side search over the whole
    archive. Matches word/name/place/date (e.g. "June", "2016") across title +
    body; results grouped by year (newest first, Undated last) with highlighted
    snippets; favorite + source filters; deep-linkable via /search?q=term.
  - Library gains a "Search across your pages ->" link (next to the timeline
    link).

Note: search is client-side BY DESIGN — entry bodies are encrypted at rest, so
the server cannot search them. The browser searches the archive it already loads,
which keeps the words private. No server-side text index is added (that would
require storing searchable plaintext).

Steps:
1. SYNC the code to origin (head b5d2881). Do not edit app code or prompts.
2. pnpm install — no-op.
3. NO drizzle-kit push (no schema change).
4. RE-PUBLISH the deployment.

Verify:
  - Visit /search, type a word that appears in your pages: results group by year
    with the match highlighted; clicking a result opens the full page. Try a year
    ("2016") and a month name ("June"). The favorite toggle and source dropdown
    narrow results.
