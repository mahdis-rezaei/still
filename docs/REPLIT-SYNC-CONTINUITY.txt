Re-sync this workspace to the latest of GitHub branch claude/ecstatic-wright-y4npO
(head 01a306e), same method as before. This is a CODE-ONLY sync — NO database
migration and NO new secrets.

What lands (first Batch 3 feature):
  - GET /api/continuity — archival "continuity" metrics (the anti-streak):
    page count, span in years, oldest-page age, writing-since year, reflection
    count, plus a couple of gentle true-now milestones. Pure aggregate read over
    unencrypted columns (no decryption, no engine).
  - A quiet ContinuityCard at the top of the Library page (silent for an empty
    archive). No streaks, no guilt — celebrates accumulation, not consistency.

Steps:
1. SYNC the code to origin (head 01a306e). Do not edit app code or prompts.
2. pnpm install — no-op (no new deps).
3. NO drizzle-kit push needed — no schema change.
4. RE-PUBLISH the deployment.

Verify:
  - GET https://yadegarjournal.com/api/continuity returns 200 with JSON
    (pageCount, writingSinceYear, spanYears, oldestPageAgeYears, reflectionCount,
    oldestImportedAgeYears, wroteFirstReflectionToday).
  - On an account with entries, the Library page shows a quiet card at the top,
    e.g. "You've been writing here since 2019" + "412 pages · spanning 3 years ·
    your oldest page is 8 years old". An empty archive shows no card.
