Re-sync this workspace to the latest of GitHub branch claude/ecstatic-wright-y4npO
(head 7d2e73e), same method as before (native GitHub pull / Contents API).

NOTE: this range still includes the api-server change (request body-size limit),
so the SERVER process must be restarted/redeployed — a client-only rebuild is not
enough.

This single prompt SUPERSEDES the earlier completeness re-sync. None of the
changes below are live yet (the deployed import page still says ".txt or .md"),
so this one publish brings everything current.

SCOPE (accurate — please read):
- Mostly frontend, PLUS two new backend endpoints and a regenerated API client.
- NO database migration. The new endpoints reuse the existing journal_entries
  table and the existing source = "sample" value, so the schema is unchanged.
- NO new secret/env var.
- There ARE two new npm dependencies (pdfjs-dist, mammoth) for in-browser file
  parsing — the lockfile is committed, so `pnpm install` must run before the
  client build.

What's in this publish:
1. Library: month + source filters (a calm "filing line": written / imported /
   samples · any year · any month) with a "clear" affordance.
2. Entry detail: a per-page "Export" action (downloads the page + its reflections
   as plain text, entirely client-side).
3. Settings: a privacy-promise line under "Your data."
4. Onboarding: a fourth door, "Let me see how it feels first," which seeds a few
   sample pages and lands on Today.
5. Library: a subtle banner when sample pages are present, with "Remove them."
6. New backend endpoints: POST /api/entries/samples (idempotent seed) and
   DELETE /api/entries/samples (soft-delete clear).
7. Import: now accepts PDF and Word/Google-Doc (.docx) files in addition to
   .txt/.md. Text is extracted IN THE BROWSER (pdf.js / mammoth), so the import
   API contract is unchanged (still sends rawText). Scanned images / handwriting
   photos are not supported (the page says so; paste instead).
8. Import review: large pastes (years of entries) are now legible — a span
   summary ("Spanning 2015–2026…"), a "Keep N pages" action at the top as well
   as the bottom, and year dividers down the list.
9. Import 413 fix: a multi-year paste exceeded Express's default 100kb body
   limit and returned a raw "Payload Too Large" page. The api-server body limit
   is now 10mb, and the import UI shows a friendly message (with "bring it in a
   few chunks" guidance) instead of leaking the raw HTTP error.
10. Import review browsability: each page can expand to its full text ("Read
    full page" / "Show less"), and year tabs (All / 2026 / … / Undated) filter
    the review to one year at a time. The year filter is a view only — "Keep"
    still keeps every checked page across all years.
11. Regenerated lib/api-zod + lib/api-client-react from lib/api-spec/openapi.yaml.

Steps:
1. SYNC the code to origin (head 51e8f62). Do not edit app code.
2. pnpm install  (this WILL fetch pdfjs-dist and mammoth — let it).
3. NO migration. If you run drizzle-kit push out of habit it should report
   "No changes detected." If it proposes ANY change (drop/rename/alter), STOP
   and tell me — the schema did not change.
4. RE-BUILD the client and RE-PUBLISH so the live app serves the updated screens
   and the api-server exposes the two new routes.
   - Manual client build if needed:
       pnpm --filter @workspace/still run build
     (build-time env the vite config expects: PORT and BASE_PATH, e.g.
       PORT=5173 BASE_PATH=/  — these are build-time only, not runtime.)
5. VERIFY after publish:
   - New routes (any signed-in session):
       POST   /api/entries/samples  -> 200, JSON array of ~5 sample entries
       (call again -> same ones, no duplicates)
       DELETE /api/entries/samples  -> 204
   - Library shows the "Showing … from … " filter line; picking a source/year/
     month narrows the shelf; "clear" resets.
   - Open any entry -> "Export" downloads a .txt of the page.
   - Settings -> the privacy-promise line appears under "Your data."
   - Onboarding (fresh account or /onboarding): "Let me see how it feels first"
     adds sample pages and drops you on Today; the Library sample banner's
     "Remove them" clears them.
   - Import: upload a small PDF and a .docx -> text is extracted and you reach
     the review screen. Paste a multi-year archive with date headings (e.g.
     "May 25, 2026") -> it splits into many dated pages, the review shows the
     span + year dividers + a top "Keep" button.
   - Large paste: paste a very long archive (hundreds of entries) -> it should
     reach the review screen, NOT a 413 "Payload Too Large". (Confirms the
     api-server restarted with the new 10mb body limit.)
   - In the review: click "Read full page" on an entry -> it expands to the full
     text; the year tabs along the top switch which year you're scanning.

Report what you ran and anything that looked off. Do not change application code
or prompts. No DB migration, no secrets, no cron changes for this publish.
