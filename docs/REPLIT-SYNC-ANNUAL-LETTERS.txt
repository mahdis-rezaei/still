Re-sync this workspace to the latest of GitHub branch claude/ecstatic-wright-y4npO
(head ae636a3), same method as before. CODE-ONLY — NO migration, NO new secrets.

What lands (Batch 3 Tier B, no-migration):
  - GET /api/letters/:year — assembles "Your Year in Pages" from existing signals
    (page count for the year, the favorited pages that stayed, reflections
    written). No engine, read-only.
  - /letters/:year — a typeset, printable letter page: cover + opening line + the
    pages, with prev/next-year navigation and a "Print / Save as PDF" button.
    App chrome is hidden when printing, so the PDF is just the letter (no server
    PDF dependency — uses the browser's print-to-PDF).
  - Library gains a "Your year in pages ->" link.

Steps:
1. SYNC the code to origin (head ae636a3). Do not edit app code or prompts.
2. pnpm install — no-op.
3. NO drizzle-kit push (no schema change).
4. RE-PUBLISH the deployment.

Verify:
  - GET https://yadegarjournal.com/api/letters/2026 returns 200 JSON (year,
    pageCount, reflectionCount, favorites[]).
  - Visit /letters/<a year you have pages in>: a cover ("Your Year in Pages" +
    the year), "This year you wrote N pages…", the favorited pages, and a
    reflections line if any. Prev/next-year arrows work. "Print / Save as PDF"
    opens the print dialog with only the letter (no nav/buttons).
