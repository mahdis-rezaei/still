Please re-sync this Replit workspace with the latest of the GitHub branch claude/ecstatic-wright-y4npO. There are new commits since your last sync. IMPORTANT: as before, preserve your local engine — do NOT overwrite artifacts/api-server/src/routes/still.ts (the ~82KB two-pass engine) or the prompts (PROMPT_VERSION 2026-06-02.9). The new commits do not change the engine, only the frontend and the auth route, so this should be a clean update.

Pull these changes from the branch:

1. A landing-page redesign (frontend only):
   - new file artifacts/still/src/components/site-chrome.tsx
   - new file artifacts/still/src/pages/why.tsx
   - updated artifacts/still/src/pages/login.tsx (now a hero + inline auth form)
   - updated artifacts/still/src/App.tsx (adds a public /why route)
   - updated artifacts/still/src/index.css (adds an ambient gradient field)

2. A security fix to artifacts/api-server/src/routes/auth.ts: the register
   handler now returns 409 for ANY existing email (it no longer links a
   password or opens a session on an existing account). This matches the fix
   you already made live — confirm your auth.ts register logic matches the
   branch (409 on existing email, no Set-Cookie, no password linking).

After syncing: run pnpm install, regenerate the API client if needed
(pnpm --filter @workspace/api-spec run codegen), run the full typecheck, then
rebuild and restart both services. No new DB migration is required.

Then confirm: the landing page shows the new "Still" hero and the "Why Still"
link works, and the register security behavior is still correct (new email
-> 201, existing email -> 409 with no cookie). Report what you ran and any errors.
