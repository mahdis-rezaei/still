Re-sync this workspace to the latest of GitHub branch claude/ecstatic-wright-y4npO
(head 3350757), same method as before. CODE-ONLY (frontend) — NO migration, NO
new endpoints, NO new secrets.

What lands (Batch 3 Tier B, no-migration):
  - /calendar — "Calendar": a 12-month paper grid showing how many pages fall in
    each month (across all years); tapping a month lists that month's pages
    across the years (grouped by year, day in the margin), each opening the full
    page. Pure frontend over the existing GET /entries data — full archive by
    date (navigation), distinct from "Look back" (the floored resurfacing view).
  - Added "Calendar" to the Explore nav menu.

Steps:
1. SYNC the code to origin (head 3350757). Do not edit app code or prompts.
2. pnpm install — no-op.
3. NO drizzle-kit push (no schema change).
4. RE-PUBLISH the deployment.

Verify:
  - Explore -> Calendar. Months with pages show a count; tap one to see its pages
    grouped by year; tap a page to open it. A month with no pages shows a gentle
    "No pages from <month> yet."
