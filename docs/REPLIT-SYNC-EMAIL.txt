Re-sync this workspace to the latest of GitHub branch claude/ecstatic-wright-y4npO
(head 29463dd), same method as before (native GitHub pull / Contents API). This
adds email verification (soft / non-blocking) and password reset via Resend.

This sync REQUIRES a small database migration (new column + new table).

Steps:
1. Sync so the workspace files exactly match origin/claude/ecstatic-wright-y4npO
   (head 29463dd). Do not edit application code.

2. pnpm install (refresh only — no new deps; email is sent via the Resend REST
   API with fetch, no SDK).

3. DB migration — adds users.email_verified (boolean) and a new auth_tokens
   table. Run:
   pnpm --filter @workspace/db exec drizzle-kit push
   (Confirm the additive changes: a new column with a default and a new table.
   No data loss.)

4. Confirm env is present (already set): RESEND_API_KEY, EMAIL_FROM
   ("Yadegar <hello@yadegarjournal.com>"). For email LINKS to point at the real
   site, APP_URL should be the public URL (the *.replit.app for now, or
   https://yadegarjournal.com once the domain is attached).

5. Rebuild + restart both services. Then RE-PUBLISH the deployment so the live
   app picks up the new code (Publish again).

6. Verify and report:
   - Register a NEW account → you should land in onboarding, and the app should
     show a soft "Confirm your email to keep your pages safe — Resend link"
     banner (it does NOT block usage).
   - If the Resend domain is verified, you should receive a "Confirm your email"
     message. Click its link → it opens /verify-email → "Your email is
     confirmed" → the banner disappears on next load.
     (If the Resend domain isn't verified yet, the send may fail/sandbox — that's
     a Resend setup matter, not a code bug. The app still works; the banner stays.)
   - Sign out → on the sign-in screen click "Forgot password?" → enter the email
     → "Check your email" → open the reset link → set a new password → sign in
     with it.

Report what you ran and any errors. Do not change application code or prompts;
if a code fix is needed, tell me and I'll fix it in GitHub.
